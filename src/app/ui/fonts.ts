import { Inter, Yeseva_One, Tangerine } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'], variable: '--font-inter' });
export const tangerine = Tangerine({
  weight: '400',
  subsets: ['latin'],
});
export const yesevaOne = Yeseva_One({
  weight: '400',
  subsets: ['latin'],
});
